startCommand:
  type: http
  configSchema:
    type: object
    properties:
      apiKey:
        type: string
        description: "Your SchedulesMCP API key. Use smcp_demo_public for synthetic demo data."
        default: "smcp_demo_public"
runtime: remote
remote:
  url: "https://mcp.schedulesmcp.com/mcp"
